# Vaultwarden Notes

- Configuration is env-file based.
- `config.json` was not present at collection time.
- The public domain is intentionally redacted in docs.
- Healthcheck URLs are intentionally redacted in docs.
- The main update script handles both server and web-vault updates.
- Keep secrets out of the repo.
- Verify `/opt/vaultwarden/data` backups after changes.
- Verify the Vaultwarden version after updates.